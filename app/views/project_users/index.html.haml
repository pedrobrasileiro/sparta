#project-users
  %ul
    = render :partial => 'user', :collection => @users

  Add user:
  - semantic_form_for @project, :html => { :class => 'add-user-form' }, :remote => true do |f|
    = f.inputs :user
    = f.submit 'Add User'
