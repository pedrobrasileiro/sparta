<h1>Listing ticket_types</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Color</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @ticket_types.each do |ticket_type| %>
  <tr>
    <td><%= ticket_type.name %></td>
    <td><%= ticket_type.color %></td>
    <td><%= link_to 'Edit', edit_project_ticket_type_path(@project, ticket_type) %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Ticket type', new_project_ticket_type_path %>
