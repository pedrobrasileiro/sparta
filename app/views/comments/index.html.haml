%h1 Listing comments
%table
  %tr
    %th Body
    %th
    %th
    %th
  - @comments.each do |comment|
    %tr
      %td= comment.body
      %td= link_to 'Show', [@project, @ticket, comment]
      %td= link_to 'Edit', edit_project_ticket_comment_path(@project, @ticket, comment)
      %td= link_to 'Destroy', [@project, @ticket, comment], :confirm => 'Are you sure?', :method => :delete
%br/
= link_to 'New Comment', new_project_ticket_comment_path
