- content_for :content do
  #header
    #logo
      = link_to '', root_path, :class => 'ribbon'
      = link_to image_tag('sparta.png'), root_path

    #navigation= render "layouts/navigation_#{body_id}"

  #content
    .content-wrapper
      = yield

= render 'layouts/base'
